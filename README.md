# GenAI Chatbot Platform

`App.js` is the main chat component
it handles user input, renders message bubbles, and contains the End Chat button that will redirect participants to the Qualtrics post-survey.


# To run locally
npm install
npm start

